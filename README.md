To run this example, you need a C++ compiler and CMake.

To configure and build this project run

```sh
cmake -B build/ -S .
cmake --build build/
```

You can then run the example with

```sh
./build/brownian_agent
```
